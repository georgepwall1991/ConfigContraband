<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <IsRoslynComponent>true</IsRoslynComponent>
    <RootNamespace>ConfigContraband</RootNamespace>
    <AssemblyName>ConfigContraband</AssemblyName>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);RS1038;RS1037;CS1591</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <PackageId>ConfigContraband</PackageId>
    <Version>0.8.0</Version>
    <Authors>George Wall</Authors>
    <Title>ConfigContraband — Options and appsettings validation analyzer</Title>
    <Description>Compile-time Options validation for .NET: a Roslyn analyzer that checks BindConfiguration, ValidateOnStart, ValidateDataAnnotations, and appsettings.json against your IOptions types so broken configuration fails in the editor and CI—not production.</Description>
    <!-- NuGet search/detail cards: short summary separate from the longer Description. -->
    <PackageSummary>Compile-time Options and appsettings validation analyzer for .NET (BindConfiguration, ValidateOnStart, ValidateDataAnnotations).</PackageSummary>
    <Summary>$(PackageSummary)</Summary>
    <PackageTags>configuration;options;appsettings;validation;ValidateOnStart;ValidateDataAnnotations;BindConfiguration;IOptions;options-pattern;DataAnnotations;roslyn;roslyn-analyzer;analyzers;static-analysis;aspnetcore;csharp;dotnet</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageIcon>assets/configcontraband-icon.png</PackageIcon>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/georgepwall1991/ConfigContraband</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/georgepwall1991/ConfigContraband</PackageProjectUrl>
    <PackageReleaseNotes>Added CFG010: bound appsettings scalars that convert but fail DataAnnotations ([Range], length, AllowedValues/DeniedValues) now warn at build time when ValidateDataAnnotations() is proven.</PackageReleaseNotes>
    <DevelopmentDependency>true</DevelopmentDependency>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <!-- Roslyn 4.8 is the .NET 8 / Visual Studio 17.8 compatibility floor.
         Keep shipped analyzer references at this baseline so older compiler hosts can load them. -->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Workspaces" Version="4.8.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="5.6.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ConfigContraband.Core\ConfigContraband.Core.csproj" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>ConfigContraband.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
    <None Include="$(OutputPath)\ConfigContraband.Core.dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
    <None Include="buildTransitive\ConfigContraband.props" Pack="true" PackagePath="buildTransitive\ConfigContraband.props" />
    <None Include="..\..\docs\nuget-analyzer.md" Pack="true" PackagePath="\README.md" />
    <None Include="..\..\assets\**\*" Pack="true" PackagePath="assets\" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="AnalyzerReleases.Shipped.md" />
    <AdditionalFiles Include="AnalyzerReleases.Unshipped.md" />
  </ItemGroup>

</Project>
